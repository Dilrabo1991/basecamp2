<%= form_with(model: tg) do |form| %>
  <% if tg.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tg.errors.count, "error") %> prohibited this tg from being saved:</h2>

      <ul>
        <% tg.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3 row">
    <%= form.label :name,  class: "col-sm-2 col-form-label", style: "display: block" %>
    <div class="col-sm-10">
    <%= form.text_field :name, class: "form-control" %>
    </div>
  </div>

  <div class="mb-3 row">
    <%= form.label :description,  class: "col-sm-2 col-form-label", style: "display: block" %>
    <div class="col-sm-10">
    <%= form.text_area :description , class: "form-control"%>
    </div>
  </div>

  <div class="mb-3 row">
    <%= form.text_field :project_id, type: :hidden %>
  </div>

  <div>
    <%= form.submit class:"submit" %>
  </div>
 
<% end %>
