<%= form_with(model: tger) do |form| %>
  <% if tger.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tger.errors.count, "error") %> prohibited this tger from being saved:</h2>

      <ul>
        <% tger.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3 row">
    <%= form.label :title, style: "display: block" %>
    <div class="col-sm-10">
    <%= form.text_field :title, class: "form-control" %>
    </div>
  </div>

  <div class="mb-3 row">
    <%= form.label :tg_id, style: "display: block" %>
    <div class="col-sm-10">
    <%= form.text_field :tg_id, type: :hidden, class: "form-control" %>
    </div>
  </div>

  <div class="createbutton">
    <%= form.submit class:"submit" %>
  </div>
<% end %>
